@model List<Model.EF.Menu>
<div class="call">
    <p><span>Cần trợ giúp?</span> gọi ngay <span class="number">0983 543 169</span></span></p>
</div>
<div class="account_desc">
    @{
        var session = (UserLogin)Session[OnlineShop.Common.CommonConstants.USER_SESSION];
    }
    @if (session == null)
    {
        <ul>
            @foreach (var item in Model)
            {
                <li><a href="@item.Link" target="@item.Target">@item.Text</a></li>
            }

        </ul>
    }
    else
    {
        <ul>
            <li>Xin chào:@session.Username</li>
            <li>@Html.ActionLink("Thoát", "Logout", "User")</li> @*linktext, actionName, controllerName*@
        </ul>
    }

</div>
<div class="clear"></div>
